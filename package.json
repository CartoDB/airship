{
  "name": "@carto/airship",
  "version": "0.0.0",
  "description": "CARTO Airship framework",
  "main": "index.js",
  "repository": "git@github.com:CartoDB/airship.git",
  "author": "Carto Dev Team",
  "license": "BSD-3-Clause",
  "devDependencies": {
    "chokidar": "^2.0.4",
    "concurrently": "^3.6.0",
    "lerna": "^2.11.0",
    "serve": "^9.2.0",
    "vinyl-fs": "^3.0.3"
  },
  "scripts": {
    "build": "lerna run --parallel build",
    "dev": "npm run clean:www && concurrently 'lerna run --parallel dev' 'node ./scripts/copy-components-html-to-www.js' --kill-others",
    "test:components": "lerna exec --scope @carto/airship-components -- npm run test",
    "test:components:watch": "lerna exec --scope @carto/airship-components -- npm run test:watch",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "postinstall": "lerna bootstrap --hoist",
    "serve": "lerna run --parallel serve",
    "test:watch": "lerna run --parallel test:watch",
    "test": "lerna run test",
    "clean:www": "rimraf www"
  },
  "dependencies": {
    "rimraf": "^2.6.2"
  }
}
